from setuptools import setup
from setuptools import find_packages

setup(
    name = "avcs",
    description = "A version control system abstraction layer.",
    author = "Brian Balsamo",
    packages = find_packages()
)
